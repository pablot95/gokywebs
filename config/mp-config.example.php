<?php
/**
 * Plantilla de credenciales de Mercado Pago.
 *
 * Copiá este archivo como `mp-config.php` (en la misma carpeta) y pegá el
 * access token real. `mp-config.php` NUNCA se commitea (está en .gitignore) —
 * en Hostinger se sube a mano una sola vez por FTP/File Manager, junto al
 * resto del sitio.
 *
 * Access token: Mercado Pago → Tu negocio → Configuración → Credenciales
 * de producción → Access Token.
 */

define('MP_ACCESS_TOKEN', 'PEGÁ_ACÁ_EL_ACCESS_TOKEN_DE_PRODUCCIÓN');
